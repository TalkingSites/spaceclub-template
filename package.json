{
  "name": "space-club-template",
  "version": "1.2.0",
  "description": "A flexible Eleventy-based static site template that’s easy to edit, easy to deploy, and easy to take anywhere.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @11ty/eleventy && npx pagefind --site _site",
    "start": "npx @11ty/eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "dayjs": "^1.11.18"
  }
}
